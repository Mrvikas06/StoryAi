name = "storytime-app"
compatibility_date = "2024-01-01"

[env.production]
routes = [
  { pattern = "storytime-app.pages.dev", zone_name = "pages.dev" }
]

[pages]
build_command = "cd frontend && npm install && npm run build"
build_output_dir = "frontend/dist"
root_dir = "."
